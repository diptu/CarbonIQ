# k8s

Status: planned — not yet implemented.

Raw Kubernetes manifests (as an alternative to the Helm charts under `infrastructure/helm/`) for environments that need direct `kubectl apply` deployment.
